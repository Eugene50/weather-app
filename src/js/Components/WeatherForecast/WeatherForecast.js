import Component from "../../framework/Component";

export default class WeatherForecast extends Component {
  render() {
    return [
      {
        tag: "h1",
        classList
      }
    ];
  }
}
