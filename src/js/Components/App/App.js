import {Temperature} from "../Temperature";

export default class App {
   constructor(host) {
      host.innerHTML = "Me weather:)";
     /*  const temperature1 = document.createElement('div');
      const temperature2 = document.createElement('div');

      new Temperature(temperature1);
      new Temperature(temperature2);

      host.append(temperature1, temperature2); */

   }
}
